name: Build Android APK
on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Unzip source if present
        run: |
          if [ -f "atun-translator-source.zip" ]; then
            unzip -o atun-translator-source.zip -d .
            echo "Unzipped source files into repo root"
          fi
          ls -la

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install deps
        run: npm install

      - name: Clean and add Android platform (always fresh)
        run: |
          rm -rf android
          npx cap add android

      - name: Patch build.gradle files to support Kotlin
        run: python3 scripts/patch-kotlin.py

      - name: Patch AndroidManifest.xml
        run: python3 scripts/patch-manifest.py

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Copy native Kotlin sources (runs LAST, after sync)
        run: |
          PKG_DIR="android/app/src/main/java/com/atun/translator"
          mkdir -p "$PKG_DIR"
          # Xoá MainActivity.java mặc định do `cap add` sinh ra để tránh
          # duplicate class với MainActivity.kt tự viết
          rm -f "$PKG_DIR/MainActivity.java"
          cp native/MainActivity.kt "$PKG_DIR/MainActivity.kt"
          cp native/OverlayWindowPlugin.kt "$PKG_DIR/OverlayWindowPlugin.kt"
          cp native/OverlayView.kt "$PKG_DIR/OverlayView.kt"
          cp native/ForegroundService.kt "$PKG_DIR/ForegroundService.kt"

      - name: Debug - verify MainActivity actually has registerPlugin
        run: |
          PKG_DIR="android/app/src/main/java/com/atun/translator"
          echo "=== Contents of $PKG_DIR ==="
          ls -la "$PKG_DIR"
          if [ -f "$PKG_DIR/MainActivity.java" ]; then
            echo "FAIL: MainActivity.java van con ton tai song song voi .kt -> se bi duplicate class."
            exit 1
          fi
          if ! grep -q "kotlin-android" android/app/build.gradle; then
            echo "FAIL: android/app/build.gradle chua duoc patch de ho tro Kotlin."
            exit 1
          fi
          echo "=== MainActivity.kt content ==="
          cat "$PKG_DIR/MainActivity.kt"
          echo "=== Checking for registerPlugin line ==="
          if grep -q "registerPlugin" "$PKG_DIR/MainActivity.kt"; then
            echo "OK: registerPlugin found in file on disk before build."
          else
            echo "FAIL: registerPlugin NOT found. Aborting build."
            exit 1
          fi

      - name: Build debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Package output as zip
        run: |
          mkdir -p release
          cp android/app/build/outputs/apk/debug/app-debug.apk release/atun-translator-debug.apk
          cd release
          zip -r atun-translator-build.zip atun-translator-debug.apk

      - name: Upload zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: atun-translator-build
          path: release/atun-translator-build.zip

      - name: Upload raw APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
